# 公式排版优化路线图

版本：v2.0
状态：非默认实验路线 / 可编码设计基线
更新时间：2026-09-10
当前结论：已完成文本公式白名单 OMML 原型、FormulaIR/载体盘点前置、角色化 OMML 样式和一个真实课件的显式 MathType/OLE → OMML 迁移试点；专用图片公式识别、图片自动归一化、闭世界无人逐条确认和自动 MathType 源提取仍未实现。

> 范围说明：本文记录非默认公式实验链路。项目默认主线、产品边界和实际开发优先级以 [`产品需求与工程路线图.md`](产品需求与工程路线图.md) 为准。默认工作流不以 LaTeX、OCR、SVG 或 OMML 转换为目标，也不得因公式实验扩大对教学内容和页面结构的修改范围。

## 1. 需求边界

本路线图只处理初中物理 PPT 中的公式排版可读性、公式载体迁移和复核效率，不改变教学内容。路线图的“全量”含义是为每个公式对象生成明确处理归宿，不是承诺每个对象都能无人工来源地转换为 OfficeMath。

自动化允许处理：

- 普通文本框中的短公式候选。
- 低风险独立公式的字体、字号、颜色、对齐和文本框宽度。
- 固定白名单公式的 MathML/OMML 候选片段导出和受控写回。
- 复杂公式的 SVG/图片渲染兜底方案。
- 公式候选、处理结果、跳过原因和回滚点报告。
- OfficeMath、MathType/OLE、普通文本、图片和混合图片的载体盘点。
- 专用公式识别模型的本仓样本评测和可替换 Adapter。
- `ClosedWorldUnattended`：对已版本化白名单命中的对象取消逐条人工点击，但保留结构、宿主和视觉门禁。

自动化默认不处理：

- 图片、视频截图中的公式识别和重写。
- MathType/OLE 对象全自动迁移。
- 组合对象内部公式拆解。
- 嵌在长句中的公式语义转换。
- 任意 LaTeX/UnicodeMath 到 OfficeMath 的无白名单批量转换。
- 未经 FormulaIR、受控规则和独立门禁验证的图片公式 OCR 到 OfficeMath 自动替换。

以下对象即使进入“全量盘点”，也不默认强制转为 OfficeMath：识别不确定的低清图片、公式和图形混合图片、无可靠结构化源的 MathType/OLE、复杂或宿主不兼容的公式。它们必须进入 `OriginalKept`、`FallbackSvg` 或 `ManualRequired`。

## 2. 技术栈依据

- PowerPoint COM：继续作为现有批处理入口，负责读取形状、应用低风险样式、导出 PDF 和页面图。
- OfficeMath/OMML：作为重要公式的长期目标格式，适合 PowerPoint 内继续编辑；本仓已有课件中可见 `a14:m` + `m:oMath` 结构，可作为后续写入样板。
- MathML：Microsoft 365 可将 Presentation MathML 导入为 Office Math，也可从 Office Math 导出 MathML，因此适合作为 OCR/TeX 与 OMML 写入之间的中间复核格式。
- LaTeX：Microsoft 365 支持将受限 LaTeX 数学表达式转换为 OMML；但 LaTeX 命令覆盖有限，不能作为无审核全自动入口。
- UnicodeMath：作为白名单公式的稳定线性表示，便于人工复核和生成 MathML/OMML 候选片段。
- MathJax/KaTeX SVG：只作为复杂公式或兼容性失败时的展示型降级方案，优先保证课堂播放视觉稳定，保留源码便于重生成。
- 图片公式 OCR：只能作为候选生成链路，必须先裁剪/识别/归一化到 FormulaIR，再进入白名单/受控语法和门禁；默认不替换图片。
- RapidOCR：继续作为通用文字探测和检索键，不升级为二维数学结构识别器。
- PP-FormulaNet：优先评测 `PP-FormulaNet_plus-M/L`；官方当前资料说明 plus-M/L 增加中文公式支持，但本仓必须自行建立 gold set，不能直接采用供应商成绩。
- UniMERNet：作为真实场景、屏幕截图和复杂公式的对照模型；资源占用和本地部署成本必须单独记录。
- pix2tex/LaTeX-OCR：作为拉丁数学公式基线或备用识别器；中文角标和单位必须单独统计，不能只看总体分数。
- Pandoc：只作为 LaTeX/MathML 到 OMML 的参考转换腿和测试夹具生成器，不作为既有 PPTX 原位重写器。
- `mathml2omml`、`node-latex-to-omml`：作为可替换转换 Adapter 候选，正式纳入前检查版本、许可证、依赖、PowerPoint 封装和回归样本。
- 现有报告链路：沿用 CSV、PDF、页面图和复核索引，新增公式分类与处理证据。

## 3. 目标架构

公式处理按“识别 -> 规范化 -> 策略决策 -> 目标生成 -> 写入 -> 验证 -> 回滚”拆成独立步骤。外部调用方只应通过 `FormulaPipeline` seam 使用这些能力，不能跨过策略层直接把模型输出交给 PPTX 写入器。

目标数据流：

```text
PPTX 载体盘点
  -> 图片裁剪/文本提取
  -> Recognition Adapter（RapidOCR / PP-FormulaNet / UniMERNet / pix2tex）
  -> FormulaIR（canonical 内容真值）
  -> PhysicsPolicyValidator（白名单、语法、上下文、单位、维度）
  -> Target Adapter（OfficeMath / SVG / 保留原图）
  -> PPTX 副本写入
  -> Open XML + PowerPoint COM + PDF/PNG + 视觉门禁
```

`FormulaPipeline` 是一个 deep module。其 Interface 应保持小而稳定，识别模型、转换器、PowerPoint 写入细节和报告拼装隐藏在 Implementation 内；`OfficeMathAdapter`、`SvgAdapter` 和 `RecognitionAdapter` 是可替换 Adapter。

模块归宿：

- `config/physics-ppt-style.config.json`：公式字体、字号、颜色和默认开关。
- `tools/Normalize-PhysicsPpt.ps1`：公式候选分类、低风险文本公式样式归一、跳过报告。
- `tools/Invoke-PhysicsPptWorkflow.ps1`：沿用 PDF、页面图和复核索引汇总。
- `tools/Export-FormulaWhitelistSuggestions.ps1`：从 `formula-review.csv` 生成白名单晋升建议，只输出复核证据，不自动改配置。
- `tools/Export-FormulaOmmlCandidates.ps1`：把白名单复核项导出为 MathML 和 `a14:m/m:oMath` 候选片段，不改 PPTX。
- `tools/Apply-FormulaOmmlWhitelist.ps1`：在 PPTX 副本中把精确匹配的白名单文本段落替换为 `a14:m/m:oMath`，原 PPTX 不覆盖。
- `tools/FormulaOfficeMathValidator/`：用 Open XML SDK 验证 PPTX 包结构，并统计 `a14:m` 与 `m:oMath`。
- `tools/Export-FormulaImageCandidates.ps1`：从图片候选清单中筛出疑似公式/物理变量标注图，导出原图和联系表，不改 PPTX。
- `tools/Invoke-FormulaImageOcrProbe.ps1` + `tools/formula_image_ocr_probe.py`：用本地 RapidOCR 对图片候选做只读 OCR 探测，输出复核 CSV/JSON/联系表。
- 后续 `tools/Formula/` 或同等内部模块：承载 FormulaIR schema、规范化、策略判定和 Adapter；只有在 F0/F1 证明跨脚本复用后才创建目录，避免为计划提前铺设空壳。
- `tools/Export-PptxVisualAudit.ps1`：导出 PPTX 每页 PNG/PDF，扫描形状越界、疑似文本溢出、小字号、空白页和公式文本，生成视觉审查 CSV/JSON/联系表。
- `tools/Apply-PptxVisualAuditFixes.ps1`：读取视觉审查 CSV，只对可确定的形状越界做副本平移修复；不删页、不改文字、不改原文件。
- 后续 `RemoteVisionReviewer`：可选接入联网视觉模型，对导出的页面图做二审；不得替代本地规则门禁，也不得直接改 PPTX。
- 后续 `OfficeMathWriter`：把当前脚本原型收敛为可复用 `OfficeMathAdapter`，按 PowerPoint 的 `a14:m` 封装写入，并增加更完整的 PowerPoint/WPS 验证。
- 后续 `FormulaRecognition`：只负责图片公式候选裁剪、候选识别、top-k 输出和结果归一化，不直接写 PPTX。
- 后续 `FormulaIR`：保存原始对象/图片哈希、识别候选、canonical TeX/MathML/UnicodeMath、token 结构、上下文、公式角色、策略结果和证据路径。
- `docs/公式处理说明.md`：面向使用者的公式处理原则。
- `docs/公式排版优化路线图.md`：实施批次、任务清单、验证门禁和回滚点。
- `vba/`：保持离线宏报告能力，后续只做等价低风险增强。

## 4. 分阶段迁移

### P0 公式分类报告增强

目标：不改 PPT 内容，只把 `FormulaCandidate` 拆出更可用的分类。

任务：

- 增加 `FormulaCandidateClass` 报告项。
- 区分低风险独立文本公式、含分式文本公式、角标样文本、希腊字母/单位符号、长句内公式。
- 保持图片、组合对象、OLE 和复杂对象只报告不处理。

验证门禁：

- 自检脚本通过。
- `-ReportOnly` 输出中能看到公式分类。
- 没有新增 PowerPoint 打开修复风险。

回滚点：删除新增分类逻辑，恢复只输出 `FormulaCandidate`。

### P1 低风险文本公式样式归一

目标：只处理普通文本框中的短独立公式，不转换语义。

任务：

- 对低风险独立公式设置数学字体、公式蓝、合适字号、居中。
- 保留原文本内容。
- 文本框只允许已有安全宽度扩展规则处理，不主动移动对象。
- 输出 `FormulaTextStyleNormalized` 或 `FormulaStyleSkipped`。

验证门禁：

- 自检脚本通过。
- 小样本导出 PDF 和页面图。
- 公式未消失、未越界、未被改写。

回滚点：关闭 `rules.formulaTextStyleDefault` 或恢复输入 PPTX。

### P2 白名单公式转换

目标：把固定物理公式从线性文本转换为可编辑 OfficeMath。

任务：

- 建立白名单映射，如 `P=W/t`、`I=U/R`、`R=U/I`、`η=W有/W总`。
- 每条映射记录源文本、目标 UnicodeMath/OMML、页码、形状名。
- P2a 先导出 MathML 和 OMML 候选片段，不改 PPTX。
- P2b 再做 PPTX 副本写入：只替换精确匹配的公式段落，插入 OfficeMath，不覆盖原文件。
- P2c 通过 PowerPoint 打开、导出 PDF、页面 PNG 视觉审查且错误数不高于基线后，才允许扩大范围。

验证门禁：

- `formula-review.csv` 中每个转换项都有白名单命中和目标表达式。
- MathML/OMML XML 片段可被 XML 解析器加载。
- 每个被转换公式有页面渲染图、联系表和结构化审查结果。
- PowerPoint 打开不触发修复。
- Open XML 校验通过，视觉审查错误数不高于转换前基线。

回滚点：只删除本批输出文件，不覆盖原文件。

### P3 复杂公式 SVG 渲染兜底

目标：对不可稳定转成 OfficeMath 的复杂公式提供展示型高质量方案。

任务：

- 调研并固定 MathJax/KaTeX 渲染参数。
- 生成透明 SVG 或 PNG fallback。
- 将公式源码写入报告或对象元数据，便于后续重生成。
- 当前先落地命令行 SVG 渲染器 `tools/Render-FormulaSvg.mjs`，由 MathJax v4 负责 TeX 到 SVG。
- 下一步副本试验使用 `tools/Apply-FormulaSvgWhitelist.ps1`，读取 `formula-review.csv` 并只替换 `ReviewWhitelistConversion` 项。

验证门禁：

- SVG 在目标 PowerPoint 版本中显示正常。
- PDF 导出清晰。
- 低版本 PowerPoint 或 WPS 有 fallback。
- 原 PPTX 不覆盖，原文本框隐藏不删除，失败项保留原状并写报告。

回滚点：保留原公式对象或原文本框，SVG 输出单独生成。

### P4 图片公式识别到可编辑公式

目标：把图片公式从“不可编辑图片”迁移为“可复核源码 + 可编辑 OfficeMath”，但只走候选、归一化和视觉审查门禁。

任务：

- 先用现有图片候选清单筛出高边缘、低色彩、疑似文字/公式图片。
- 为图片公式输出裁剪图、所在页、媒体文件、候选等级和 OCR 待处理状态。
- 当前先用 RapidOCR 做免费本地文字探测，区分公式/变量标注、普通物理图示和人工目检项。
- 真正的图片公式识别先评测 `PP-FormulaNet_plus-M/L`，再用 UniMERNet 和 pix2tex/LaTeX-OCR 做对照；模型名称不是采纳结论，必须以本仓真实裁剪样本的 canonical FormulaIR 准确率、错误接受率、中文角标/单位错误率、CPU/GPU 成本和许可证为依据。
- `PP-FormulaNet_plus-M/L` 作为中文课件优先候选，`UniMERNet` 作为真实截图/复杂公式对照，pix2tex 作为拉丁数学公式基线；不得把 RapidOCR 直接升级成公式内容源。
- 识别 Adapter 输出 top-k LaTeX/MathML 候选、置信度、模型版本、权重哈希和预处理信息，结果先进入 FormulaIR，不直接写回 PPTX。
- FormulaIR 必须由白名单或受控语法生成 canonical TeX/MathML/UnicodeMath；识别结果只能作为检索键和候选证据。
- 归一化到白名单或受控公式源后，才可复用 P2 的 MathML/OMML 候选片段和 OfficeMath 写入链路。
- 对无法稳定识别的图片保留原图，最多提供 SVG/PNG 展示兜底。

验证门禁：

- 每个图片公式候选必须有裁剪图和原图引用。
- OCR 结果必须能回显为 MathML/SVG，并在写回前通过 FormulaIR、结构、宿主和视觉审查门禁。
- 不得用 OCR 结果直接覆盖原图。
- 模型评分不能单独触发写回；至少需要 canonical 内容信号、结构信号和视觉信号三类证据。

回滚点：删除 OCR 候选输出和新 PPTX 副本，原 PPTX 与原图片保持不变。

### P5 复核门禁工作流

目标：把不可自动判断的公式集中为可筛选的复核清单，把可自动判断的白名单公式交给视觉审查门禁。

任务：

- 生成公式复核清单和自动门禁结果。
- 按页码、形状名、公式文本、分类、建议动作排序。
- 从复核清单聚合白名单建议，按表达式完整性、频次、文件覆盖数和现有配置覆盖情况分级。
- 对高风险项只给建议，不自动执行。
- 将 `FormulaStyleSkipped`、`FormulaWhitelistCandidate`、`FormulaTextStyleFailed` 纳入重点复核页。
- 对 `ReviewWhitelistConversion` 项记录 OMML 写入、Open XML 校验、视觉审查基线和自动门禁状态。
- 明确四类决策模式：`ReportOnly`、`CandidateOnly`、`ClosedWorldUnattended`、`ExplicitMigration`；未实现的模式不能出现在可运行参数或成功报告中。
- `ClosedWorldUnattended` 只允许白名单/受控语法命中项，且要求识别结果与页面上下文、边界框和原图哈希相容；它取消逐条点击，不取消机器门禁。
- 每个拒绝项必须给出确定性原因：未命中、候选冲突、中文角标歧义、单位/维度失败、目标不支持、视觉失败、宿主失败或证据缺失。

验证门禁：

- 复核清单能直接定位页面。
- 每个跳过项有原因。
- 每个自动通过项有 `automationGateStatus=Passed` 和可追踪证据。
- 批次汇总能分别统计 `Converted`、`FallbackSvg`、`OriginalKept`、`ManualRequired`、`Skipped` 和 `Failed`，不能只报告转换数量。

回滚点：不修改 PPT，仅删除报告。

### P6 视觉审查与自动修复闭环

目标：把“我能看见页面效果”的审查能力落成可重复门禁，而不是一次性人工目检。

任务：

- 用 PowerPoint 官方导出链路生成 PDF 和每页 PNG。
- 对每页 PNG 做本地视觉指标：空白页、内容贴边、黑白/非白占比、联系表。
- 用 PowerPoint COM 读取形状元数据：越界、文本边界疑似溢出、小字号、公式文本。
- 生成自动确认层：按页给出通过、源课件已知问题、需复核、失败，并输出带状态边框的联系表。
- 有基线时只阻断新增错误；源课件已有空白页、贴边等问题进入 `PassedWithKnownIssues`，不误判为本次回归。
- 自动确认层比较页数、错误/警告类型、页面非白/白色/深色占比变化；同页同严重级别问题即使形状名变化，也按基线问题处理，降低误报。
- 对可安全修复的问题只写入副本；对删除空白页、移动对象、缩放对象等改变教学流程或布局的动作，必须作为单独批次和回滚点。
- 自动修复只允许“与画布相交且无需缩放”的越界形状平移；完全离开画布的对象、需要缩放的大图和可能是有意裁切的图片，只报告不改动。
- 可选联网 AI API 只做二审：读取页面图，输出 JSON 审查意见；本地无 API key 时不影响主流程。
- VLM 不得同时作为唯一生成器和唯一裁判；至少保留白名单/FormulaIR 和确定性结构门禁。

验证门禁：

- 每个输出 PPTX 都有 PDF、页面 PNG、视觉审查 CSV、形状审查 CSV、自动确认 CSV/JSON、联系表和 manifest。
- 视觉审查脚本不能修改原文件。
- 自动确认层只能把新增错误阻断，把新增警告列为复核；不能直接改写 PPTX。
- AI API 输出只能进入报告，不能直接写回 PPTX。

回滚点：删除视觉审查输出或删除修复副本，原 PPTX 不变。

### P7 FormulaIR 与受控公式源

目标：建立跨识别器、转换器和写入器的稳定语义接口，使模型可以替换而不改变 PPTX 写回安全边界。

任务：

- 定义 `FormulaIR` 的 JSON Schema，区分 source、recognition、canonical、policy、target、evidence 和 decision。
- 保存原始图片/文本哈希、页面、形状、裁剪边界、候选列表、模型/权重版本和预处理参数。
- 保存 token 结构：变量、数字、运算符、单位、中文角标、上标、下标、分式、根式、括号和文本标签。
- 建立 canonical TeX、Presentation MathML 和 UnicodeMath 的互相生成/规范化规则；原始 OCR 字符串不能替代 canonical 源。
- 为白名单增加允许的源载体、上下文、公式角色、单位定义、温差约定、隐式乘法策略和版本状态。
- 将白名单维护与 PPTX 写回分离；白名单变更只生成建议和校验结果，不静默修改配置。

验证门禁：

- Schema 能拒绝缺少来源哈希、canonical 源、决策原因或证据路径的记录。
- 同一 FormulaIR 能稳定生成 LaTeX、MathML、UnicodeMath 和候选 OMML。
- 归一化重复运行结果一致；别名和空白归一化不改变语义 token。
- 未命中或候选冲突只能产生 `ManualRequired`/`OriginalKept`，不得产生 `Converted`。

回滚点：删除 FormulaIR schema/测试和新增报告字段，保留现有 `formula-review.csv` 兼容读取。

### P8 公式识别模型评测与 Adapter

目标：在真实中文物理课件上选择识别器，而不是依据通用宣传或单一 BLEU 分数选型。

任务：

- 从现有图片候选中建立带 canonical FormulaIR 的 gold set，覆盖清晰截图、低清图片、中文角标、单位、分式、负号、根式、公式与图形混合区域。
- 为每个样本保存原图、裁剪图、页面/形状引用、人工审定 canonical 源和难度标签；gold set 不随普通输出目录混入版本控制。
- 实现统一的 `RecognitionAdapter` 输入/输出协议，至少支持 RapidOCR 探测、PP-FormulaNet_plus-M/L、UniMERNet 和 pix2tex 基线。
- 对每个模型分别记录完整公式正确率、FormulaIR exact match、中文角标准确率、单位准确率、分式/上下标准确率、错误接受率、CPU/GPU 延迟、峰值内存和模型体积。
- 使用当前仓库实际样本复核供应商 benchmark；不把 BLEU 作为唯一指标，至少增加结构或渲染级指标。
- 模型缺失、权重哈希不匹配、运行时异常或输出 JSON 无效时，降级为 `CandidateOnly`，不得阻断其他文件且不得伪造通过。

验证门禁：

- 每个 Adapter 都能对同一输入产生 schema 合法的 JSON。
- gold set 上没有未解释的高风险误接受；发布阈值由样本校准并写入配置/manifest。
- 任何模型的中文角标、单位或负号错误率超过策略阈值时，只能作为候选，不得进入无人逐条确认模式。

回滚点：卸载或删除实验模型 Adapter、权重和可选 Python 环境，RapidOCR 只读探针继续可用。

### P9 转换 Adapter 与 OfficeMath 写入

目标：把 LaTeX/MathML/UnicodeMath 转换和 PPTX 写回分别隔离，保持可替换性和回滚性。

任务：

- 并排评估现有自研 tokenizer、MathJax + `mathml2omml`、`node-latex-to-omml` 和 Pandoc 对照链。
- 将 Pandoc 限定为新文档/测试夹具和转换结果对照，不用它整体重写既有 PPTX。
- 将 OMML 写入收敛为 `OfficeMathAdapter`，生成符合 PowerPoint `a14:m` 封装的片段，并复用现有 `FormulaOfficeMathValidator`。
- 对复杂公式记录 OMML 的归一化损失；不支持或视觉不稳定时转 `FallbackSvg` 或 `OriginalKept`。
- 禁止新增 `MathTypeAdapter` 直接生成 MTEF；MathType 只接受可靠的结构化导出源或显式人工提供的 canonical 源。

验证门禁：

- 每个转换器的结果都能经过 XML 解析和 canonical 结构对照。
- 同一 FormulaIR 在不同 Adapter 上的差异有明确报告，不能静默选择未知结果。
- OMML 副本 PowerPoint 打开不触发修复，页数、对象几何、文本、动画、媒体和关系差异只包含已授权变化。

回滚点：删除转换 Adapter 和 OMML 副本，输入 PPTX 和原对象不变。

### P10 角色化样式与无人逐条确认试点

目标：先解决公式写回的排版和自动接受策略，再扩大图片覆盖范围。

任务：

- 把当前候选生成器中的固定 `3800`、全量斜体和单一 Latin 字体改为角色/ token 驱动的样式策略。
- 使用 `formulaInline`、`formulaStandalone`、`formulaCore` 生成基础字号，并对公式框边界、基线和邻近对象做有限调整。
- 变量/希腊字母、数字/运算符、单位、中文角标分别指定字体和直立/斜体策略。
- 只对封闭白名单命中项启用 `ClosedWorldUnattended`；开放公式和候选冲突项自动降级。
- 在至少一个真实课件上完成图片公式小批量副本，输出原图、裁剪图、canonical 源、OMML/SVG、PDF/PNG、结构报告、视觉报告和 manifest。
- 统计误接受、漏接受、原图保留率、SVG 降级率和 PowerPoint 打开失败率；不得只统计转换成功数。

验证门禁：

- 文字、图片和 MathType 三种来源均有正向、负向和失败降级样本。
- 自动接受样本不存在未解释的内容误改；有任何内容误改，立即退出无人逐条确认模式。
- 新公式区域无新增裁切、溢出、重叠、乱码或基线异常。

回滚点：关闭实验模式，删除本批输出副本和证据目录，恢复原 PPTX。

### P11 MathType 显式迁移与范围评审

目标：只在 OfficeMath 试点稳定后，处理用户明确要求迁移的 MathType/OLE 资产。

任务：

- 盘点 OLE 程序标识、可编辑状态、关联文件和目标宿主版本。
- 优先使用 MathType/Office 能够导出的 MathML/LaTeX 或人工提供的 canonical 源。
- 对每个迁移对象保存 OLE 原始哈希、结构化源、目标 OMML、原/新渲染图和几何差异。
- 把 MathType 迁移做成单独输出目录和显式参数，不并入默认规范化副本。
- 迁移失败、源缺失或视觉不稳定时保留 MathType OLE，不强制降级为错误图片。

验证门禁：

- MathType 输入、OfficeMath 输出和保留原状三类结果均可复现。
- 不直接写入或篡改 MTEF；所有目标公式经过 FormulaIR 和 PowerPoint 实际验证。
- 用户明确要求的目标 Office/WPS 版本均完成打开和导出测试后，才评审是否扩大迁移。

回滚点：删除迁移副本，恢复原 MathType OLE；不修改源 PPTX。

## 5. 当前最小执行范围

本次执行到 P2c 样本闭环，并推进 P4 只读候选/OCR 前置：

- 增加公式候选分类报告。
- 增加低风险独立文本公式样式归一。
- 增加公式白名单识别和转换建议报告。
- 增加独立 SVG 渲染烟测工具。
- 增加白名单公式的 MathML/OMML 候选片段导出工具。
- 增加白名单公式写入 PPTX 副本的 OMML 原型，并用 Open XML + PowerPoint COM 做样本验证。
- 增加图片公式候选筛选、候选图导出、联系表和 RapidOCR 只读探测报告。
- 增加 PPTX 视觉审查脚本，导出 PDF/页面 PNG 并检查空白页、越界、文本溢出、小字号和公式文本。
- 增加低风险视觉修复脚本，只把越界形状平移回画布内，输出新 PPTX。
- 增加视觉自动确认脚本，把视觉审查结果转为按页状态、自动门禁和确认联系表，并在有基线时区分源课件既有问题与本次新增问题。
- 增加白名单建议报告，把全量 `formula-review.csv` 聚合为可晋升/需复核/下标待看/已覆盖/拒绝候选。
- 收紧低风险视觉修复策略：不再缩放大图，不把完全画布外对象移入页面，避免破坏有意裁切和隐藏素材。
- 将增强一键默认切到中速闭环：跳过预检 ReportOnly、原始/对比逐页图、全量视觉审查、视觉修复和 OMML 视觉审查；保留规范化、PDF、公式复核、白名单建议、OMML 副本和 Open XML 校验。
- 深度视觉审查保留为显式开关：`-IncludeReviewArtifacts -IncludeVisualAudit -ApplyVisualAuditFixes -FormulaOmmlVisualAudit`。
- 修复白名单快照滞后问题：OMML 候选导出时优先使用当前 `config/physics-ppt-style.config.json` 中的目标公式。
- 修正“功”公式白名单目标为初中教材常见写法：`W=Fs` / `W=Fs`；显式乘点只作为必要时的人工复核选项。
- 继续禁止无白名单的公式语义转换。
- 继续禁止图片公式、组合对象和 OLE 自动替换。
- 本次文档更新不改变上述默认范围；专用公式模型和 `ClosedWorldUnattended` 只有完成 F0–F9 的相应门禁后，才可进入显式实验参数。

## 6. 依赖与工具状态

已可用：

- `.NET SDK 10.0.300`：本机用户目录安装，路径为 `C:\Users\sciman\.dotnet\dotnet.exe`，用于 Open XML SDK 验证器。
- `DocumentFormat.OpenXml 3.3.0`：由 `tools/FormulaOfficeMathValidator` 项目引用，用于 PPTX 结构验证。
- Pandoc 3.9.0.2 便携版：路径为 `tools/vendor/pandoc/pandoc-3.9.0.2/pandoc.exe`。Pandoc 可把 docx/pptx 数学渲染为 OMML，适合作为后续批量转换参考，但当前 PPT 原位编辑仍以 Open XML 写入器为主。
- MathJax v4：用于 TeX 到 SVG/MathML 的展示兜底和复核渲染。
- Codex bundled Python：已包含 Pillow、OpenCV、NumPy、onnxruntime、rapidocr_onnxruntime，可支撑免费本地 OCR 探测。

未强制安装：

- Tesseract 与 ImageMagick：`winget` 非交互安装不稳定，本轮没有把它们作为必需依赖。
- PP-FormulaNet/PP-FormulaNet_plus：需要单独评估 Paddle/PaddleX 运行时和模型权重，不等同于直接替换 RapidOCR。
- UniMERNet：需要单独 Python/Transformers 运行时和模型权重；官方仓库列出的模型体积较大，不作为默认安装项。
- pix2tex/LaTeX-OCR：开源可用，但依赖 Torch/Transformers 和较大模型；在图片候选链路跑通前不作为默认安装项。
- `mathml2omml`、`node-latex-to-omml`：只作为转换 Adapter 候选；正式纳入前完成许可证、依赖锁定、PowerPoint 封装和回归测试。
- 联网视觉 API：默认关闭；若启用，必须显式授权、记录服务/模型/请求版本，并只作为报告二审。

## 7. 任务清单

- [x] P0：公式候选分类与复核报告。
- [x] P1：低风险独立文本公式样式归一。
- [x] P2 安全前置：白名单命中与 `formula-review.csv`。
- [x] P2a：导出 MathML/OMML 候选片段，不改 PPTX。
- [x] P2b 原型：`Apply-FormulaOmmlWhitelist.ps1` 在 PPTX 副本中插入 `a14:m/m:oMath`。
- [x] P2c 样本验证：Open XML 验证、PowerPoint 打开、PDF 导出、第 2 页 PNG 导出。
- [x] P2d 白名单扩展建议：从全量 `formula-review.csv` 生成可晋升/需复核建议清单，不自动改配置。
- [x] P2e 中速 OMML 闭环：默认只做白名单写入和 Open XML 校验，逐页视觉审查改为显式重型开关。
- [x] P4 前置：图片公式候选清单、候选图导出、OCR 复核报告和联系表。
- [x] P6 前置：PPTX 全页视觉审查、页面 PNG/PDF 导出、规则审查 CSV/JSON/联系表。
- [x] P6 低风险修复：对视觉审查发现的第 50 页越界图片生成修复副本并复测。
- [x] P6 自动确认 v2：基线页数、视觉指标变化和同页问题匹配进入门禁，批量跑通 `PPTX` 目录 10 个课件。
- [x] P6 性能分层：全量逐页视觉审查退出默认增强一键，只作为深度复核模式。
- [ ] P3：SVG/PNG 展示兜底只用于不可编辑或兼容失败项。
- [ ] P4 扩展：图片公式 LaTeX/MathML 专用识别、归一化和候选报告。
- [ ] P5：人工复核 UI 或更易读的审核表。
- [ ] P6 扩展：可选联网视觉 API 二审、批量修复副本和差异验收。

### 公式专项编码任务

以下任务与 [`公式识别转换实施计划.md`](公式识别转换实施计划.md) 的任务卡一一对应。每次 AI 执行只领取一个任务，完成后必须提交规定证据；未实现的任务不得在报告中标记为已支持。

- [x] `F0-01` 固定公式载体枚举、决策状态和兼容字段。
- [x] `F0-02` 定义 `FormulaIR` JSON Schema 和最小合法样例。
- [x] `F0-03` 定义 `formula-evidence-manifest.json` 字段、路径相对化规则和哈希绑定。
- [x] `F0-04` 定义 `ReportOnly`、`CandidateOnly`、`ClosedWorldUnattended`、`ExplicitMigration` 模式及默认关闭策略。
- [x] `F1-01` 扫描并统计 `a14:m`/OfficeMath、MathType/OLE、文本、图片、组合对象和混合图片。
- [x] `F1-02` 为图片候选保存原始媒体哈希、页面、形状和边界框。
- [x] `F1-03` 将已有 RapidOCR 探针输出映射为候选证据，不改变现有只读行为。
- [x] `F1-04` 建立图片公式区域裁剪、padding、缩放和原图回溯协议。
- [ ] `F2-01` 建立真实课件公式裁剪 gold set 和人工审定 canonical 源。
- [ ] `F2-02` 接入 `PP-FormulaNet_plus-M` 评测 Adapter。
- [ ] `F2-03` 接入 `PP-FormulaNet_plus-L` 或明确记录资源不足的降级结果。
- [ ] `F2-04` 接入 UniMERNet 对照 Adapter。
- [ ] `F2-05` 接入 pix2tex 基线 Adapter，单独统计中文角标和单位错误。
- [ ] `F2-06` 记录模型/权重/运行时/设备/预处理/延迟/内存/许可证。
- [ ] `F2-07` 以 FormulaIR exact match、错误接受率和高风险 token 错误率形成选型报告。
- [ ] `F3-01` 实现 LaTeX/MathML/UnicodeMath 到 FormulaIR 的规范化。
- [ ] `F3-02` 实现 FormulaIR 到 canonical LaTeX/MathML/UnicodeMath 的稳定生成。
- [ ] `F3-03` 实现中文角标、单位、负号、分式、隐式乘法和上下标规则。
- [ ] `F3-04` 实现上下文约束，禁止只凭 OCR 字符串在同形白名单项之间盲选。
- [ ] `F3-05` 实现未命中、候选冲突和规则失败的确定性降级。
- [x] `F4-01` 并排验证自研 tokenizer、MathML-to-OMML 和 Pandoc 对照输出。
- [ ] `F4-02` 固定转换器版本、依赖锁和许可证证据。
- [ ] `F4-03` 对 OMML 不支持或归一化损失的公式转 SVG/原图并报告。
- [ ] `F5-01` 将现有 OMML 原型收敛为角色化 `OfficeMathAdapter`。
- [ ] `F5-02` 修复当前 `3800`/全量斜体/单一 Latin 字体的硬编码。
- [ ] `F5-03` 按 inline/standalone/core 和 token 类型注入字号、字体、直立/斜体。
- [ ] `F5-04` 保持 `a14:m` 封装、对象几何、层级、动画、媒体和原对象回滚。
- [ ] `F5-05` 增加 OMML 结构故障注入和 PowerPoint 修复提示检测。
- [ ] `F6-01` 生成 canonical 源对应 SVG，验证透明边距、基线和裁切。
- [ ] `F6-02` 定义 `FallbackSvg` 与 `OriginalKept` 的报告和回滚语义。
- [ ] `F7-01` 实现白名单命中图片的 `ClosedWorldUnattended` 计划模式，但先禁止写回。
- [ ] `F7-02` 用真实样本验证内容、结构、宿主、视觉四层门禁。
- [ ] `F7-03` 小批量通过后才允许图片公式 OMML 副本写回。
- [ ] `F7-04` 发现一次未解释的错误接受后，自动关闭无人逐条确认模式。
- [ ] `F8-01` 评估 MathType/OLE 的可靠结构化源提取路径。
- [ ] `F8-02` 实现 MathType 显式迁移副本和独立 manifest，不生成 MTEF。
- [ ] `F8-03` 迁移失败时保留 MathType OLE 并输出原因。
- [ ] `F9-01` 增加全量归宿汇总：Converted/FallbackSvg/OriginalKept/ManualRequired/Skipped/Failed。
- [ ] `F9-02` 增加跨运行重复性、缓存和 evidence hash 校验。
- [ ] `F9-03` 形成目标 Office/WPS 版本矩阵和真实样本回归报告。
- [ ] `F9-04` 更新使用说明、风险边界、回滚说明和 AI 任务清单。

## 8. 本轮验证证据

- `tools/Test-ToolkitFiles.ps1` 通过。
- `reports/pptx_dir_enhanced_visual_v3` 已对 `PPTX` 目录 10 个课件重新执行增强一键处理：报告、规范化、PDF、视觉审查、低风险修复、视觉自动确认和 OMML 白名单副本流程均完成。
- 10 个课件的修复后视觉自动确认门禁均为 `Passed`；剩余视觉审查错误均按基线识别为源课件既有问题，没有新增阻断项。
- `reports/pptx_dir_fast_formula_v1` 已对 `PPTX` 目录 10 个课件执行中速公式闭环：耗时 `00:11:33`，10/10 成功，PDF 10 个，输出校验异常 0，默认跳过全量逐页视觉审查。
- `reports/fast_formula_single_smoke` 已对 `12.4机械效率（王耀强）.pptx` 执行中速单课件冒烟：耗时 `00:01:25`，OMML 写入 1 个，Open XML 错误 0，自动门禁通过 1 / 0。
- `reports/fast_formula_classroom_style_smoke` 已复测课堂写法修正：白名单公式 `W   =   F ∙  s` 的 OMML 候选目标为 `W=Fs` / `W=Fs`，不强制乘点；OMML 写入 1 个，Open XML 错误 0，自动门禁通过 1 / 0。
- `reports/fast_formula_subscript_style_smoke` 已按“中文角标本来就是下标”的规则复测：单课件中速闭环耗时约 `00:01:58`，OMML 候选 14 / 14，写入 14 个；`W总=Fs` 转为 `W_总=Fs`，`W有＝G物h` 转为 `W_有=G_物h`，`W额 = W总 -W有` 转为 `W_额=W_总-W_有`；Open XML 错误 0，自动门禁通过 1 / 0。
- `formula-review.csv` 汇总结果：`ManualReview=100`，`TextStyleNormalized=395`，`ReviewWhitelistConversion=1`；OMML 白名单写入只处理 1 条已配置白名单，不做无白名单语义转换。
- `Export-FormulaWhitelistSuggestions.ps1` 已从全量复核清单生成白名单建议：272 个公式组，导出 120 条建议，其中 `PromoteCandidate=9`、`ReviewCandidate=111`。
- 白名单建议已改为教材展示写法优先：例如 `W总=Fs` 转为可编辑下标 `W_总=Fs` / `W_{\text{总}}=Fs`，但不自动改成 `W_总=F·s`。
- `Export-FormulaOmmlCandidates.ps1` 已从 `formula-review.csv` 生成 1 条白名单公式的 MathML/OMML 片段。
- `Apply-FormulaOmmlWhitelist.ps1` 已对 `PPTX/12.4机械效率（王耀强）.pptx` 生成 OMML 副本，并写入 1 个 `a14:m/m:oMath` 公式。
- `FormulaOfficeMathValidator` 验证该 OMML 副本：`SlideCount=53`，`A14MathCount=19`，`OfficeMathCount=19`，`OpenXmlErrorCount=0`。
- PowerPoint COM 能打开该 OMML 副本，识别 53 页，导出 PDF，并导出第 2 页 PNG。
- 第 2 页 PNG 目检通过：白名单公式已以可编辑 OfficeMath 形态显示，页面未空白、未触发修复。
- `Export-FormulaImageCandidates.ps1` 已从 `reports/final_image_candidates/pptx-image-candidates.csv` 生成图片公式候选报告：129 / 269 张图片进入复核候选。
- `Invoke-FormulaImageOcrProbe.ps1` 已对候选中的 20 张图片做 RapidOCR 探测：20 张成功，6 张进入公式/变量文本复核，10 张进入物理标注复核，4 张保留人工目检。
- `Export-PptxVisualAudit.ps1` 已对 OMML 副本生成 `reports/formula_omml_visual_audit`：53 页 PNG、PDF、视觉审查 CSV、形状审查 CSV 和联系表。
- 视觉审查发现：第 49 页为空白页；对比原始 PPTX 后确认该空白页来自源课件，不是 OMML 写入回归。
- 视觉审查结论已修正：初中课件中 `W_总=Fs` 这类角标加隐式乘法属于常见写法，不默认改为显式乘点；后续只在来源明确或人工确认时保留 `·` / `×`。
- `Apply-PptxVisualAuditFixes.ps1` 已生成 `reports/formula_omml_visual_fixed/12.4机械效率（王耀强）.formula-omml.visual-fixed.pptx`，把第 50 页两个底部越界图片平移回画布内。
- 修复副本复测：`OpenXmlErrorCount=0`；视觉审查阻断项从 3 个降至 1 个，仅剩第 49 页源课件空白页。

## 9. 风险与回滚

关键风险：

- 公式字体变化导致中文角标显示不一致。
- 文本框扩宽后与相邻对象距离变小。
- 个别 PowerPoint 版本对数学字体回退不同。
- 手写 PPTX 中的 OMML 结构不完整，导致 PowerPoint 打开时触发修复。
- 图片公式 OCR 误识别，导致教学公式含义错误。
- 图片候选筛选会把物理图示、坐标尺、表格等纳入候选，不能把 OCR 文本直接当成公式。
- 规则视觉审查会产生保守告警，例如图示贴边、小幅文本边界超出；这些应进入复核表，不应默认自动改版。
- 联网视觉 API 可能误判空间位置或小字，应作为二审建议而不是唯一判断。
- “白名单命中”只能在白名单本身正确且上下文约束通过时作为内容真值；不能声称能消除所有错误。
- “视觉等价”只能证明渲染外观接近，不能单独证明物理语义等价。

控制方式：

- 只处理低风险独立文本公式。
- 默认不改公式内容。
- 报告每个处理结果。
- 输出新 PPTX，不覆盖原文件。
- OMML 写入前先生成片段和复核清单，写入后必须通过 PowerPoint 打开与 PDF 对比。
- 图片公式默认只输出候选和 OCR 待复核结果，不直接替换。
- RapidOCR 当前只做文字探测，不是专用数学公式识别模型；专用公式 OCR 必须另设模型评测门禁。
- 任何专用模型必须先经过本仓 gold set 评测；供应商 BLEU/CDM 或单次 VLM 置信度不能直接授权写回。
- 无人逐条确认只允许用于封闭白名单和受控公式集；开放公式仍然进入 `ManualRequired` 或保留原图。
- 所有视觉修复先保存新 PPTX；默认只做低风险平移，不做缩放、不删除空白页、不把完全画布外对象移回页面。

回滚动作：

- 设置 `rules.formulaTextStyleDefault=false`。
- 删除本次输出目录中的规范化文件。
- 回到 `_backup_originals` 或原输入 PPTX。

## 10. 参考依据

- Microsoft Learn：[MathML Support in Microsoft 365](https://learn.microsoft.com/office/math/mathml)，说明 PowerPoint/Word/Excel/OneNote 支持 MathML 与 Office Math 的导入导出。
- Microsoft Learn：[LaTeX Support in Microsoft 365](https://learn.microsoft.com/office/math/latex)，说明 Microsoft 365 可将受限 LaTeX 数学表达式转换为 OMML。
- Microsoft Learn：[Microsoft 365 Math Documentation](https://learn.microsoft.com/office/math/)，说明 Microsoft 365 Apps 使用 OMML 存储数学，并支持 MathML/LaTeX 互操作。
- Microsoft Learn：[Open XML SDK for Office](https://learn.microsoft.com/office/open-xml/about-the-open-xml-sdk)，说明 Open XML 是可编程处理 PPTX/DOCX/XLSX 的标准包结构，并支持强类型对象、查找、修改和验证。
- Microsoft Learn：[Presentation.ExportAsFixedFormat method](https://learn.microsoft.com/office/vba/api/powerpoint.presentation.exportasfixedformat)，说明 PowerPoint 可将演示文稿发布为 PDF/XPS 固定格式，适合作为视觉审查基准。
- Microsoft Learn：[PowerPoint PDF Accessibility](https://learn.microsoft.com/office/pdf/powerpoint/powerpointpdfaccessibility)，说明先按 PowerPoint 可访问性实践制作，再保留为 PDF；对本仓意味着 PDF 导出也应作为交付检查项。
- MathJax：[Converting a Math String to Other Formats](https://docs.mathjax.org/en/v4.1/web/convert.html)，说明 TeX 可转换为 SVG/MathML，适合本仓展示兜底和人工复核。
- Pandoc：[Pandoc User's Guide - Math](https://pandoc.org/MANUAL.html)，说明 Docx 和 PowerPoint 输出中的数学会渲染为 OMML。
- RapidOCR：[RapidAI/RapidOCR](https://github.com/RapidAI/RapidOCR)，提供基于 ONNX Runtime 等后端的开源、可本地部署 OCR 工具。
- pix2tex/LaTeX-OCR：[lukas-blecher/LaTeX-OCR](https://github.com/lukas-blecher/LaTeX-OCR)，提供图片公式到 LaTeX 的开源方向，可作为后续专用公式 OCR 候选。
- PaddleOCR 公式识别：[PaddleOCR Formula Recognition](https://github.com/PaddlePaddle/PaddleOCR/blob/main/docs/version3.x/module_usage/formula_recognition.md)，提供 PP-FormulaNet/PP-FormulaNet_plus 与 UniMERNet 的模型说明、输出和测试数据边界。
- UniMERNet：[opendatalab/UniMERNet](https://github.com/opendatalab/UniMERNet)，提供真实场景公式识别、数据集、模型和 CDM 评测方向。
- MathML-to-OMML：[fiduswriter/mathml2omml](https://github.com/fiduswriter/mathml2omml)，作为 JavaScript MathML 到 OMML 的候选 Adapter；正式使用前检查 LGPL 义务和 PowerPoint 封装。
- LaTeX-to-OMML：[JaredYe04/node-latex-to-omml](https://github.com/JaredYe04/node-latex-to-omml)，作为 LaTeX/MathJax/MathML/OMML 对照 Adapter，不替代本仓 PPTX 写入器。
- MathType MTEF：[WIRIS MathType MTEF](https://docs.wiris.com/en_US/mathtype-mtef-v5-mathtype-40-and-later)，说明 MTEF 是二进制格式及直接生成的风险。
- Mathpix Docs：[Mathpix OCR API](https://docs.mathpix.com/)，说明图片公式 OCR 可输出带数学内容的结构化结果，适合作为候选识别参考，不适合作为默认无审核替换。
- OpenAI Docs：[Images and vision](https://developers.openai.com/api/docs/guides/images-vision)，说明 Responses API 可把图片作为输入进行分析；本仓仅把它作为可选远程视觉二审，不作为必需本地依赖。
- 现有课件样本：`PPTX/12.4机械效率（王耀强）.pptx` 已包含 `a14:m` + `m:oMath` 的可编辑公式结构，可作为本仓 OfficeMath 写入样板。
